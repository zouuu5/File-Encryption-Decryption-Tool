# 🔐 File Encryption/Decryption Tool

A Python CLI tool to encrypt and decrypt text files using the `cryptography` library and a secret key.

## 📦 Requirements

- Python 3.x
- `cryptography` package

Install it with:

```bash
pip install cryptography
